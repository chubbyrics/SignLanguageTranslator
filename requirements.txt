# Add requirements.txt
streamlit==1.28.0
numpy==1.24.3
Pillow==10.0.0

# Add vercel.json
{
  "builds": [
    {
      "src": "sign_language_app.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "sign_language_app.py"
    }
  ]
}